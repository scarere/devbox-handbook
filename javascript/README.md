# JavaScript Packages and Package managers

The current JS package manager of choice is bun, and hence that is the only one documented here. The packages directory contains docs on useful javascript packages I have found.